# 2026-07-06 — Rota Page Built

**What happened:** built `/admin/rota`, a weekly calendar grid over the existing `jobs` table (no new schema/RPC). Cleaners as rows, days as columns, dedicated pinned "Unassigned" row. Sticky headers. Edit panel updates `jobs.scheduled_time`/`cleaner_id` via the same direct-update path already used elsewhere.

**Verification:** user confirmed live in a real browser — sidebar highlight, week nav, correct cell placement, edit round-trip all checked out. Two follow-up UX gaps (cancelled jobs still occupying grid cells; unscheduled jobs invisible) were found, fixed same day per explicit "fix everything before moving on" instruction, held for re-verification before push.

**Outcome:** pushed `cb564b1..b45594d`. This is also the day the 4-step Visual Language Upgrade spec was worked through (sidebar restructure, KPI card redesign, invoice-status donut, read-only profile pages) — each step build-verified, diff-reviewed, held for go-ahead before push. A real dark-mode contrast bug was found live by the user during step 4 and root-caused via a full grep audit (not just patching the two reported spots) — fixed once at the `<main>` level so it cascades to all descendants.
